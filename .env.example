# Required — base URL of the OpenClaw CAMIS gateway (no trailing slash).
# Example:      https://example.com
# Local dev:    http://localhost:19001
OPENCLAW_WA_SDK_BASE_URL=https://example.com

# Required — must match the gateway's API_TOKEN_WA env var.
OPENCLAW_WA_SDK_TOKEN=replace-me
